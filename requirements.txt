# ============================================================
# Churn Dashboard — runtime dependencies (app + model serving)
# Python 3.10  ·  install:  pip install -r requirements.txt
# ============================================================

# ── Web ──
Flask==3.1.3
python-dotenv==1.2.2

# ── Data ──
pandas==2.3.3
numpy==1.26.4

# ── Model serving (loads models/churn_model.joblib at /predict) ──
scikit-learn==1.5.2
joblib==1.5.3
xgboost==3.2.0
lightgbm==4.6.0

# ── LLM insight providers (optional — app runs in demo mode without keys) ──
groq==1.5.0
anthropic==0.111.0
google-generativeai==0.8.6

# ── Database (optional — app runs on the bundled CSV without it) ──
SQLAlchemy==2.0.51
oracledb==4.0.1
